{
  "name": "smtp-tester",
  "version": "1.0.0",
  "description": "Web-based SMTP testing tool with live conversation viewer",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
